function toggleMenu() {
    var navLinks = document.querySelector('.index-nav, .main-nav');
    navLinks.classList.toggle('show'); // Toggle the 'show' class for nav-links
}

// burger menu from Chat Gpt